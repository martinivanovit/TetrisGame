<UserControl x:Class="TetrisApp.GlyphButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TetrisApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <FontFamily x:Key="FontAwesome">/TetrisApp;component/Resources/#Font Awesome 5 Free Solid</FontFamily>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="textBlock"
               FontFamily="{StaticResource FontAwesome}"
               Text="{Binding Glyph, RelativeSource={RelativeSource AncestorType=local:GlyphButton}}"/>
        <ContentControl Grid.Column="1" Content="{Binding Content, RelativeSource={RelativeSource AncestorType=local:GlyphButton}}" />
    </Grid>
</UserControl>
